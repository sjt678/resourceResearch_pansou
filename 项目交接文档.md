# PanSou 项目交接文档

> **编写日期**：2026-07-28
> **更新背景**：本文档于 2026-07-25 首次编写（mipan 插件修复），2026-07-27 重大更新（mihomo 代理搭建、TG 频道配置、三数据源架构），2026-07-28 二次更新（修复"全部检测失败"+"TG 0 条"：mihomo 只听 127.0.0.1 + 检测误用代理客户端），2026-07-28 三次更新（**mihomo 配置文件 rules 坏行致启动崩溃的修复法**；**61 个插件全量实测，精简为 11 个可用插件**）。当前 HEAD: `443e864`。
>
> ✅ **2026-07-28 15:00 最终验证通过**：mihomo 删除全部 4 条 `'# GEOIP` 坏行后 `active (running)`，监听 `*:7890`，容器经 `172.17.0.1:7890` 出网返回 302；TG 频道搜索恢复——`kw=庆余年&src=tg` 返回 **102 条**（来源 `tg:ydypzyfx` 等）。**至此三大搜索源（聚合/频道/插件）+ 链接检测全部正常，无遗留问题。**

---

## 〇-1、2026-07-28 新特性：无效链接自动剔除 + 有效性统计条

**需求**：三个搜索源的失效链接全部剔除，只展示有效链接，页面提示"总共 xx 条数据，xx 条有效，xx 条无效"。

**实现（纯前端，后端无改动）**：
1. `src/composables/useSearch.js` — 搜索完成后自动**全量分批检测**（`checkAllProgressively`：每批 25 条、串行请求，防止压垮后端）。旧的"仅当前页懒检测"逻辑废弃。
2. `src/views/Home.vue` —
   - `keptList`：实时剔除 `checkStates` 为 `invalid` 的项，只展示剩余项
   - `checkStats`：统计条数据源，展示"总共 X 条数据，Y 条有效，Z 条无效"，检测未完成时附"（检测中 n/N）"进度
   - Tab 计数改用剔除后的 `keptMergedByType`，保证 Tab 数字与实际展示一致
   - 检测过程中总页数收缩时当前页自动回退（watch totalPages）

**剔除口径（重要）**：只剔除**确定失效**（后端 `bad` → 前端 `invalid`）的链接；`locked`（需提取码）、`uncertain`（暂不可知）、`unsupported`（平台不支持检测）、`error`（检测异常）**保留展示并计入"有效"**。原因：国内网盘反爬严格，大量链接无法判定但实际可用，若把"无法判定"也剔除会误杀一大半真实资源。

**注意事项**：
- 全量检测 378 条约需 1-2 分钟跑完（后端有并发池 + 24h/6h 检测缓存，重复搜索同关键词秒出）
- 统计数字在检测过程中会动态变化（无效数逐渐上升、页面条目逐渐减少），属正常现象
- 若以后想把"暂不可知"也剔除，改 `Home.vue` 里 `keptList` 的过滤条件即可（加 `'uncertain'`），但强烈不建议

---

## 〇、2026-07-28 最新修复（检测失败 / TG 0 条）

### 问题现象
1. 任何搜索源下，链接检测全部显示"检测失败"（`state: uncertain`, `summary: 检测失败`）
2. 频道搜索（`src=tg`）任何关键词都返回 `total: 0`，且响应极快（~8ms）
3. 插件搜索能出结果，但检测同样全失败

### 根因（两个叠加）
| # | 根因 | 影响 |
|---|---|---|
| A | **mihomo 默认只监听宿主机 `127.0.0.1`**（未开 `allow-lan`）。容器内 `host.docker.internal` 解析到 docker 网桥 IP（如 172.17.0.1），连接被**拒绝** | TG 搜索全部瞬间失败 → 0 条（~8ms 即返回就是"秒拒"的特征） |
| B | **检测服务误用全局代理客户端**：`check_service.go` 的 `NewCheckService` 用 `util.GetHTTPClient()`，该客户端带着 `PROXY=socks5://host.docker.internal:7890`（本为 TG 搜索配置）。代理不可达 → 所有检测瞬间失败 | 全部链接"检测失败" |

**为什么插件搜索正常**：插件（如 mipan）各自 `new http.Client` 直连，不经过全局代理客户端，国内站点直连阿里云没问题。

### 修复内容（提交 `740b656`）
1. `pansou/service/check_service.go` — 检测服务改用**专用直连客户端**（`newCheckHTTPClient()`），不再用全局代理客户端；如需代理可设 `CHECK_PROXY` 环境变量或请求传 `proxy_url`
2. `pansou/service/search_service.go` — TG 频道搜索失败**增加日志**（此前错误被静默吞掉，无法定位）：`[TG] 频道 xxx 搜索失败: ...`
3. `pansou/util/http_util.go` — 启动时输出代理配置日志

### 服务器侧还需一步（mihomo 开 allow-lan）
```bash
# 1. 开启局域网访问（容器要走网桥 IP 访问宿主机代理）
sed -i 's/^allow-lan: false/allow-lan: true/' /root/mihomo-config/config.yaml
grep -q "^allow-lan: true" /root/mihomo-config/config.yaml || sed -i '/^mixed-port:/a allow-lan: true' /root/mihomo-config/config.yaml
systemctl restart mihomo

# 2. 模拟容器视角验证（用 docker 网桥 IP 测试，不是 127.0.0.1！）
GATEWAY=$(docker network inspect pansou-web_default --format '{{(index .IPAM.Config 0).Gateway}}')
curl -x socks5://$GATEWAY:7890 -s --connect-timeout 10 "https://t.me/s/tgsearchers7" | head -c 200

# 3. 更新代码并重建
cd /root/pansou_project && git pull origin main
cd pansou-web && docker compose up -d --build

# 4. 验证
curl -s "http://localhost:8888/api/search?kw=庆余年&src=tg&refresh=true" | head -c 300   # TG 应有结果
# 注：末尾若出现 `curl: Failed writing body` 是 head 截断管道所致，不是错误，忽略即可
docker compose logs pansou 2>&1 | grep "\[TG\]" | tail -5                               # 失败会有具体原因
```

**三个搜索源的精确测试命令**（与前端按钮一一对应）：
```bash
# 聚合搜索 = mipan 单插件
curl -s "http://localhost:8888/api/search?kw=仙逆&src=plugin&plugins=mipan" | head -c 300

# 频道搜索 = TG 频道
curl -s "http://localhost:8888/api/search?kw=仙逆&src=tg" | head -c 300

# 插件搜索 = 11 个精选插件（2026-07-28 实测精简，不含 mipan）
curl -s "http://localhost:8888/api/search?kw=仙逆&src=plugin&plugins=duoduo,qupanshe,muou,wanou,ouge,ash,quark4k,hunhepan,xiaozhang,yuhuage,u3c3" | head -c 300

# 注意：src=plugin 不带 plugins 参数 = 全部 ENABLED_PLUGINS（含 mipan），
# 不对应网站任何一个按钮，条数会明显偏多（mipan 贡献大头），对比数据时别用错
```

### 插件全量实测结果（2026-07-28，提交 `443e864`）

对 61 个插件逐一调用 `src=plugin&plugins=<单个>`（关键词"仙逆"，refresh 强刷 + 缓存复读两轮）：

| 结果数 | 插件 |
|---|---|
| 7 | duoduo |
| 6 | qupanshe |
| 5 | muou |
| 4 | wanou、ouge、ash |
| 3 | quark4k |
| 2 | hunhepan、xiaozhang、yuhuage |
| 1 | u3c3 |
| **0（共 50 个）** | labi、zhizhen、shandian、jikepan、panwiki、pansearch、panta、qupansou、hdr4k、pan666、susu、thepiratebay、xuexizhinan、panyq、huban、cyg、erxiao、miaoso、fox4k、pianku、clmao、wuji、cldi、libvio、leijing、xb6v、xys、ddys、hdmoli、javdb、clxiong、jutoushe、sdso、xiaoji、xdyh、haisou、bixin、djgou、nyaa、xinjuc、aikanzy、xdpan、discourse、yunsou、qqpd、ahhhhfs、nsgame、gying、quarksoo、sousou |

**结论**：60 个插件里 50 个已死（站点失效/反爬/需代理），"插件搜索"按钮数据少是插件生态现状，不是 bug。已将前端列表与 `ENABLED_PLUGINS` 精简为 **mipan + 上述 11 个**，按钮文案改为"11个精选插件"。数据主力是 **mipan（900+ 条）+ TG 频道**。
**注意**：gying/qqpd/weibo 等走全局代理客户端的插件，在 mihomo 修好后可重新实测，若想用加回 `ENABLED_PLUGINS` 即可。
**实测脚本**：`.workbuddy/plugin_benchmark.py`（改 BASE 和 KW 可复用）。

**mihomo 重启失败排查**：
```bash
journalctl -u mihomo -n 30 --no-pager                   # 看真实报错（首选）
ss -tlnp | grep 7890                                   # 是否有僵尸进程占用端口
pkill -9 mihomo && systemctl restart mihomo            # 端口被老进程占用时
grep -c "allow-lan" /root/mihomo-config/config.yaml    # 应为 1；>1 是重复键，会导致启动失败
# 实在不行：重新下载订阅配置（必须带UA伪装）→ 重新加 allow-lan/external-controller → restart
```

### mihomo 崩溃实例：rules 里"注释写错位置"（07-28 实际遇到）

**报错**：`Parse config error: rules[2] [# GEOIP,lan,DIRECT,no-resolve] error: unsupported rule type # GEOIP`
**根因**：`config.yaml` 的 `rules:` 列表里有一行写成了 `- # GEOIP,lan,DIRECT,no-resolve`（在列表项里加 `#`）。YAML 里这不叫注释，mihomo 把整行当规则文本解析 → 启动即崩。是之前手动改配置时弄坏的；**运行中的进程不重读配置，所以一直正常，重启才暴露**（重启计数 1400+ 次就是这么来的）。

**修复（按内容定位 → 按行号删除，最稳）**：
```bash
# 1. 按内容找出坏行（别用 \s 正则，BRE grep 不支持；也别在 sed 里玩复杂捕获组，容易被终端吞转义符）
grep -n "GEOIP,lan" /root/mihomo-config/config.yaml

# 2. 看一眼长什么样（把 158 换成上一步的实际行号）
sed -n '155,160p' /root/mihomo-config/config.yaml

# 3. 删除该行（有多个坏行时从大到小删）
sed -i '158d' /root/mihomo-config/config.yaml

# 4. 重启并验证
systemctl restart mihomo && sleep 2
systemctl status mihomo | head -3     # 应为 active (running)
ss -tlnp | grep 7890                  # 应为 0.0.0.0:7890

# 5. 若又报 rules[N] 错误，说明还有别的坏行，重复 1-4；或把 journalctl 新报错发出来
```

**⚠️ 重要后续（07-28 二次踩坑）：坏行不止一处，共 4 条！**
只删 `GEOIP,lan` 那一条后 mihomo 仍崩溃循环。全文排查发现订阅生成器把 **4 条** GEOIP 规则都写成了 `- '# GEOIP,...' # disabled for offline` 的假注释形式（第 84 行 + 文件末尾 3928/3930/3931 行附近）。`grep "GEOIP,lan"` 只能匹配到第一条，剩下 3 条（telegram/CN）漏掉了。

**一次性删干净的正确姿势（不用逐条找行号）**：
```bash
cp /root/mihomo-config/config.yaml /root/mihomo-config/config.yaml.bak
grep -vF "'# GEOIP" /root/mihomo-config/config.yaml > /tmp/config.yaml && mv /tmp/config.yaml /root/mihomo-config/config.yaml
grep -c "GEOIP" /root/mihomo-config/config.yaml   # 必须输出 0 才算删干净
systemctl restart mihomo && systemctl status mihomo --no-pager
```
> 经验：遇到"某类行写坏了"的问题，先 `grep -c` 统计总数，别只修 grep 到的第一条。修复后必须再 grep 确认为 0。
> 工作区 `mihomo-config/config.fixed.yaml` 是已验证无坏行的完整修复版（3848 条规则），可直接 scp 覆盖服务器的 `/root/mihomo-config/config.yaml`。

> ⚠️ **安全提醒**：`allow-lan: true` 后 mihomo 监听 `0.0.0.0:7890`，**千万不要在阿里云安全组放行 7890 端口**（保持只开 80），否则代理会被全网扫到滥用。容器走内网网桥访问不受安全组影响。

---

## 一、项目概况（一句话版）

PanSou 是一个网盘资源聚合搜索系统（monorepo）：

- `pansou/` —— Go 后端 API（Gin），端口 8888，通过 90+ 插件聚合各搜索源
- `pansou-web/` —— Vue3 + Vite5 前端，nginx 跑在 80 端口，反向代理 `/api` 到后端 8888

本次维护只动了 **mipan** 这一个数据源插件（对接 `https://www.mipan.so`）。

**两份 docker-compose 注意**：
- `pansou/docker-compose.yml` —— 只起后端
- `pansou-web/docker-compose.yml` —— 前端 + 后端一起起（整站部署用这份）

两份里后端镜像同名 `pansou2:latest`、容器同名 `pansou`，**不要两个目录都 `up`**，会冲突。整站固定用 `pansou-web/`。

---

## 二、2026-07-27 重大进展

### 2.1 架构升级

| 项 | 旧（07-25） | 新（07-27） |
|---|---|---|
| 代理方案 | CF Worker 反代 TG（被墙） | 阿里云服务器本地 mihomo 代理 |
| 数据源 | 仅 mipan | **三数据源可切换** |
| 启用插件 | 1 个（mipan） | 61 个 → **07-28 实测精简为 12 个**（mipan + 11 个可用，见第〇节实测表） |
| 频道支持 | 118 个 TG 频道（未生效） | 118 个 TG 频道（已配置，待 mihomo 修复后验证） |

### 2.2 三数据源架构（前端可切换）

| 数据源 | UI 名字 | API 参数 | 后端行为 | 所需依赖 |
|---|---|---|---|---|
| 🚀 聚合搜索 | "极速 · 多源聚合" | `src=plugin&plugins=mipan` | 只用 mipan 一个插件 | 无（国内直连） |
| 📡 频道搜索 | "118 个 TG 频道" | `src=tg` | 只抓 TG 频道 | 需 mihomo 代理 |
| 🔍 插件搜索 | "60 个插件全网搜" | `src=plugin&plugins=labi,zhizhen,...` | 60 个插件（不含 mipan） | 部分插件可能需代理 |

**实现文件**：
- 前端切换 UI：[pansou-web/src/views/Home.vue](file:///d:/Desktop/pansou_project/pansou-web/src/views/Home.vue) - `source-switch` 组件
- 搜索逻辑：[pansou-web/src/composables/useSearch.js](file:///d:/Desktop/pansou_project/pansou-web/src/composables/useSearch.js) - `run()` 函数根据 `source.value` 传不同参数
- 数据源标签：[pansou-web/src/components/ResultCard.vue](file:///d:/Desktop/pansou_project/pansou-web/src/components/ResultCard.vue) - 卡片显示"聚合/TG频道/插件"标签

### 2.3 mihomo 代理搭建（核心）

**部署位置**：`/root/mihomo-config/` + `/root/mihomo` 二进制
**服务管理**：`systemd` 服务（`/etc/systemd/system/mihomo.service`）
**关键配置**：
- `mixed-port: 7890`（HTTP+SOCKS5 代理端口）
- `external-controller: 127.0.0.1:9090`（API 控制端口）
- `mode: rule`（规则模式，**不影响其他服务**）
- 下载配置时**必须带 UA 伪装**：`curl -L -A "ClashforWindows/0.20.39" <订阅链接>`

**完整安装指南**：[Linux安装Mihomo配置代理踩坑指南.md](file:///d:/Desktop/pansou_project/Linux安装Mihomo配置代理踩坑指南.md) — 12 个坑的详细解决方案。

**关键命令**：
```bash
# 服务管理
systemctl status mihomo
systemctl restart mihomo

# 测试代理
curl -x socks5://127.0.0.1:7890 -s --connect-timeout 10 "https://t.me/s/tgsearchers7" | head -c 200

# 查看策略组
curl -s "http://127.0.0.1:9090/proxies" | python3 -m json.tool

# 手动更新订阅（**必须带UA伪装**）
curl -L -A "ClashforWindows/0.20.39" "https://47.112.97.173:5000/api/v1/client/subscribe?token=08b65b8d5504d79d9df98d7aa20910eb" -o /root/mihomo-config/config.yaml
systemctl restart mihomo
```

**重要**：**不启用自动更新**（mmdb 下载容易卡），改成手动更新（一周一次）。

### 2.4 容器代理配置

**关键代码**（两个 docker-compose.yml 都已配置）：
```yaml
services:
  pansou:
    environment:
      - PROXY=socks5://host.docker.internal:7890   # 走宿主机的 mihomo
    extra_hosts:
      - "host.docker.internal:host-gateway"        # docker 访问宿主机
    # TG_BASE_URL 已注释掉，改用直接走代理
```

**为什么用 `host.docker.internal`**：Docker 容器内 `127.0.0.1` 指向容器自己，要访问宿主机必须用 `host.docker.internal`（Docker 17.06+ 支持）。

### 2.5 2026-07-27 新提交记录

| # | 提交 | 说明 |
|---|---|---|
| 1 | `8025906` | feat: 配置mihomo代理直接访问TG频道，弃用CF Worker反代 |
| 2 | `b872d46` | feat: 添加数据源切换(聚合搜索/深度搜索)并启用全部插件 |
| 3 | `44b1099` | feat: 数据源改为三个(聚合/频道/插件搜索) |
| 4 | `294970a` | fix: 优化检测状态显示(uncertain友好提示) + 添加数据源标签 |

**同一时间段内但提交顺序较前的相关提交**（属于本次重构上下文）：
- `4cea642` fix: 修复链接检测状态圆点全灰色和检测速度慢问题
- `af38388` chore: 主页副标题改为固定文案'聚合上百个TG频道·几十个搜索源'
- `ed59f96` fix: 每次搜索强制刷新缓存(refresh=true),避免返回旧搜索结果

### 2.6 当前已知问题（待解决）

1. ~~**TG 频道搜索返回 0 条**~~ - **已于 07-28 修复并验证**（`740b656` + mihomo 开 allow-lan + 删除 4 条 `'# GEOIP` 坏行，见第〇节；实测 `kw=庆余年&src=tg` 返回 102 条）
2. ~~**链接检测全部"检测失败"**~~ - **已于 07-28 修复**：根因是检测服务误用全局代理客户端，已改直连（`740b656`）。注意：国内网盘站反爬严，部分链接仍可能返回 `uncertain`（"暂不可知"），那是检测能力上限，不是 bug
3. **mihomo 自动更新** - 已放弃，mmdb 下载在阿里云上经常卡死，改手动更新（一周一次）

### 2.7 搜索缓存策略（refresh=true 强刷）

**决策**（2026-07-27 提交 `ed59f96`）：
- **搜索结果** —— 每次查询都强刷缓存（`refresh=true`），保证用户看到的是最新数据
- **链接检测结果** —— 仍走缓存（24h/6h 两级），避免每个链接都重新请求

**为什么这样**：
- 用户在 mipan.so 搜能秒出 378 条，但本项目第一次出 14 条 —— 罪魁祸首就是缓存命中了之前插件没修好时的"坏数据"
- 别的网站都是实时搜，缓存只对链接检测有效，搜索结果不该缓存
- 链接检测可以缓存（网盘站反爬严，频繁检测会被封 IP）

**代码位置**：[pansou-web/src/composables/useSearch.js](file:///d:/Desktop/pansou_project/pansou-web/src/composables/useSearch.js) 第 27 行 `searchParams = { kw, refresh: true, ...options }`

**注意**：
- 强刷后第一次搜索会比较慢（几秒到十几秒），第二次同关键词也慢（依然强刷）
- 这是设计如此，**不要**把 `refresh=true` 去掉

---

## 三、历史：本会话做了什么 / 已完成什么

| # | 任务 | 状态 | 关键提交 |
|---|---|---|---|
| 1 | 熟悉项目结构 | ✅ | — |
| 2 | 修复 mipan 插件 struct tag 反引号损坏导致的 Docker 编译失败 | ✅（本地早已修复，本次确认并推送） | `6309927` |
| 3 | 修复 mipan 插件"搜索结果为空"：POST+JSON → GET+query | ✅ | `1114856` |
| 4 | 推送 Gitee + GitHub（含上述修复及前端自动检测改动） | ✅ | `1114856` |
| 5 | 修复 `pansou-web/docker-compose.yml` 未启用 mipan 插件 | ✅ | `fc57a71` |
| 6 | 修复前端白屏（useSearch.js 暴露字段与 Home.vue 不匹配） | ✅ | `82ff880` |
| 7 | 定位"搜出来只有 14 条 vs 别人 378 条"：旧缓存命中 | ✅（无需改码，清缓存即可） | — |

**最终推送到的提交**：`82ff880`（Gitee: `song-juntang/Resource_research`；GitHub: `sjt678/resourceResearch_pansou`）。

**当前线上状态（2026-07-24 实测服务器 47.108.65.55）**：
- `/api/health` 正常返回
- `kw=庆余年` 普通搜索返回 **378 条**（与 mipan.so 一致），其中 `src=plugin` 单独测也是 378 条，`src=tg` 为 0 条（当前未配置 TG 频道）

---

## 四、踩过的坑（绝对不要踩）

### 🔴 坑 1：mipan.so 接口必须用 GET，不能用 POST
- **现象**：插件原先 `POST + JSON body {kw: ...}` 调 `https://www.mipan.so/api/search`，返回**空**，一条都没有。
- **真相**：实测该接口 GET 方式 `?kw=关键词` 返回完整数据（庆余年 378 条），POST 直接空。
- **已修**：`pansou/plugin/mipan/mipan.go` 的 `doSearch` 改为 GET，用 `url.QueryEscape(keyword)` 拼 `?kw=`。
- **别再踩**：不要把它改回 POST。如果以后要加分页参数（`page`/`size`），继续用 GET query。

### 🔴 坑 2：struct tag 反引号必须是标准 ASCII 反引号（0x60）
- **现象**：服务器 Docker 构建报 `syntax error: unexpected name json in struct type`，指向 mipan.go 第 102/106/111 行（正是 struct tag 行）。
- **真相**：那是**反引号不是标准字符**（全角或别的 Unicode 字符）导致 Go 解析失败。本地早已在 `6309927` 修复（反引号换成标准 ASCII），但当时分支领先远程 5 个提交没推送，服务器拉到的是坏版本。
- **别再踩**：编辑 `*.go` 的 struct tag 时，确认反引号是半角 `` ` ``。如果服务器再报类似错，先看本地文件反引号字节，再确认代码是否 push。
- **验证方法**：`sed -n '行号p' 文件 | od -c` 看反引号是不是 `0x60`。

### 🔴 坑 3：改 composable 必须同步改所有消费方，否则整页白屏
- **现象**：修复后前端整页空白，Network 面板请求都 200 正常，但页面无内容。
- **真相**：重构 `useSearch.js` 后只暴露了 `checkResults / run / cancel / reset`，但 `Home.vue` 仍按旧 API 解构 `checkStates / checkSummaries / batchCheck`，取到 `undefined`，模板访问时抛 `TypeError` 未捕获 → Vue 把整个组件干掉 → 白屏。
- **已修**：`82ff880` 把 useSearch.js 恢复成 Home.vue 期望的接口（`checkStates/checkSummaries/batchCheck`），保留"搜索后自动批量检测"行为。
- **别再踩**：**任何改动 composable / store / 公共函数后，先 `grep -rn "符号名" src/` 全仓确认所有解构消费方**。这是本次最隐蔽的坑。

### 🔴 坑 4：二级缓存会把"坏数据"缓存下来，修好插件后旧词仍显示旧结果
- **现象**：插件修好后，前端搜"庆余年"还是只有 14 条（别人 378 条）。
- **真相**：插件没修好之前搜过这词，那时"TG 14 条 + 插件 0 条"被写进缓存。修好后再搜同词仍命中旧缓存。
- **已修（无需改码）**：实测 `src=plugin` 直接调是 378 条；`refresh=true` 强刷后普通搜索也变 378 条。清缓存卷 `pansou-cache` 后全量正常。
- **别再踩**：
  - 改了数据源逻辑后，**务必清一次缓存卷**：`docker compose down` → `docker volume rm <cache卷名>` → `docker compose up -d`。
  - 单关键词强刷：`/api/search?kw=xxx&refresh=true`。
  - 卷名可能带目录前缀（如 `pansou-web_pansou-cache`），`docker volume ls | grep cache` 确认实际名字。
- **正常预期**：后端会按 URL 去重 + 标题关键词过滤，条数比 mipan.so 略少是正常的，但**量级必须一致**（几百对几百）。

### 🔴 坑 5：compose 里 `ENABLED_PLUGINS` 必须包含 mipan
- **现象**：即使插件代码正确，如果没启用也不会生效。
- **真相**：`pansou-web/docker-compose.yml` 原先 `ENABLED_PLUGINS` 是 70 多个旧插件，**不含 mipan**。
- **已修**：`fc57a71` 改为 `ENABLED_PLUGINS=mipan`。
- **别再踩**：以后加新插件，记得在 compose 的 `ENABLED_PLUGINS` 里登记，否则白忙活。

### 🔴 坑 6：两份 compose 镜像/容器同名，别两个目录都 up
- **现象**：会端口/容器名冲突。
- **别再踩**：整站部署只跑 `pansou-web/` 那份。

### 🔴 坑 7：推送 GitHub 需走代理；服务器 git pull 遇到本地改动要先丢弃
- **推送**：本机直连 GitHub SSL 握手失败，走本机代理 `http.proxy=http://127.0.0.1:7897`；Gitee 可直连。用系统 Git（`/d/Softwares/Git/Git/cmd/git.exe`）走凭据，managed git 的 GCM 没存 GitHub 凭据。
- **服务器 pull**：若报 `Your local changes to ... would be overwritten by merge`（如 mipan.go 有服务器本地改动），先 `git checkout -- pansou/plugin/mipan/mipan.go` 丢弃再 `git pull`。**远程版本是权威修复版，丢弃服务器本地改动是安全的。**

### 🔴 坑 8（07-27 新增）：mihomo 订阅下载必须伪装 User-Agent
- **现象**：直接 `wget` 或 `curl` 订阅链接，拿到的是 `vmess://eyJ2...` 一坨 base64 编码的节点列表，**mihomo 直接报错**。
- **真相**：机场订阅服务检测到命令行客户端时返回原始 vmess 列表；检测到 Clash/Mihomo 客户端时返回 YAML 配置。区分依据是 HTTP `User-Agent`。
- **已修**：必须带 UA 伪装：
  ```bash
  curl -L -A "ClashforWindows/0.20.39" "订阅URL" -o /root/mihomo-config/config.yaml
  ```
- **别再踩**：更新订阅、备份配置、调试订阅，**所有下载命令都要带 `-A` 参数**。

### 🔴 坑 9（07-27 新增）：mihomo mmdb 文件名必须严格匹配 `Country.mmdb`
- **现象**：启动 mihomo 后日志一直 `Can't find MMDB, start download`，但 `mihomo -t` 验证配置时**下载超时卡死**（GFW 拦截 GitHub）。
- **真相**：mihomo 找的是 `Country.mmdb`（C 大写，无后缀），从 GitHub 下载的 `country-lite.mmdb`（c 小写，带 -lite）**名字对不上**。
- **已修**：改名 `mv country-lite.mmdb Country.mmdb`，或从国内镜像下载。
- **别再踩**：手动下载 mmdb 后**必须确认文件名是 `Country.mmdb`**。

### 🔴 坑 10（07-27 新增）：`mihomo -t` 验证配置会卡 1 分 30 秒
- **现象**：`timeout 30 /root/mihomo -t -f config.yaml` 设置了 30 秒超时，**但实际上卡 1 分 30 秒才退出**。
- **真相**：`mihomo -t` 内部硬编码 90 秒下载超时，`timeout 30` 杀不掉子进程树。
- **已修**：放弃 `mihomo -t` 验证，直接 `systemctl restart mihomo` + `systemctl status` 看是否 active。
- **别再踩**：写自动更新脚本时，**不要用 `mihomo -t` 验证配置**，直接重启看状态。

### 🔴 坑 11（07-27 新增）：docker 容器访问宿主机代理必须用 `host.docker.internal`
- **现象**：pansou 容器内 `PROXY=socks5://127.0.0.1:7890` 配了，但**容器连不上 mihomo**。
- **真相**：容器内 `127.0.0.1` 指向容器自己，不是宿主机。要访问宿主机必须用 `host.docker.internal`。
- **已修**：
  ```yaml
  environment:
    - PROXY=socks5://host.docker.internal:7890
  extra_hosts:
    - "host.docker.internal:host-gateway"
  ```
- **别再踩**：在 docker compose 里配宿主机代理，统一用 `host.docker.internal:7890`，别用 `127.0.0.1`。

### 🔴 坑 12（07-27 新增）：链接检测后端返回 `uncertain` 不是 bug
- **现象**：所有 quark/baidu 链接检测都显示"暂不可知"。
- **真相**：后端检测代码访问分享页面时，**网盘站有反爬机制**（IP 频率限制、JS 渲染、登录墙等），导致后端拿不到有效状态。后端返回 `uncertain` 表示"无法判定"。
- **已修**：前端把 `uncertain` 独立显示为"暂不可知"（灰色圆点），不再显示"检测失败"。
- **别再踩**：看到"暂不可知"是正常情况，不是 bug。如果要更准确的检测，需要后端做 JS 渲染或换 IP 池。

### 🔴 坑 14（07-28 新增）：mihomo 必须开 `allow-lan: true`，容器才能用上代理
- **现象**：宿主机上 `curl -x socks5://127.0.0.1:7890 https://t.me/...` 完全正常，但 pansou 容器里 TG 搜索全部秒失败、返回 0 条（响应 ~8ms）。
- **真相**：mihomo/clash 默认 `allow-lan: false`，**只监听 127.0.0.1**。容器内 `host.docker.internal` 解析到 docker 网桥 IP（如 172.17.0.1），不属于回环地址，连接直接被拒。"宿主机自测正常"不能证明"容器可用"。
- **已修**：`/root/mihomo-config/config.yaml` 加 `allow-lan: true` 后 `systemctl restart mihomo`。
- **验证是否生效（先看监听地址，铁证）**：
  ```bash
  ss -tlnp | grep 7890
  # 127.0.0.1:7890 → allow-lan 没生效；0.0.0.0:7890 → 已生效
  grep -n "allow-lan" /root/mihomo-config/config.yaml   # 确认配置行真的写进去了
  ```
- **别再踩**：
  - 验证代理要给容器用时，**必须用网桥 IP 测试**（模拟容器视角），不能只测 127.0.0.1：`curl -x socks5://$(docker network inspect pansou-web_default --format '{{(index .IPAM.Config 0).Gateway}}'):7890 https://t.me/s/tgsearchers7`
  - 开了 allow-lan 后**绝不能在安全组放行 7890**（保持只开 80），否则代理暴露公网被滥用。
  - "秒失败"（远小于超时就返回）基本等于"连接被拒绝"，优先怀疑监听地址/端口，而不是目标网站被墙。
  - **本地 Docker Desktop 部署同理**：错误里的 `192.168.65.254` 是 Docker Desktop 的宿主机网关。本机若没有代理监听 compose 里配的 7890（比如本机代理实际端口是 7897），同样 connection refused。本地要测 TG：把 PROXY 改成本机代理真实端口，且本机代理软件要开"允许局域网连接/Allow LAN"。
  - **改配置后要确认真的写进去了**：sed 锚点（如 `^mixed-port:`）匹配不上时会静默失败，改完务必 `grep` 复查 + `ss -tlnp` 看监听地址。

### 🔴 坑 15（07-28 新增）：检测/普通业务不要用"为翻墙准备的全局代理客户端"
- **现象**：所有网盘链接检测全部"检测失败"，但插件搜索明明正常。
- **真相**：`check_service` 直接用了 `util.GetHTTPClient()`（带 `PROXY` 的全局客户端，那是给 TG 搜索准备的）。代理一不可达，检测全灭；就算代理可用，海外节点访问夸克/百度也更容易被风控。
- **已修**：`740b656` 检测服务改用专用直连客户端（`newCheckHTTPClient()`），代理需经 `CHECK_PROXY` 或 `proxy_url` 显式指定。
- **别再踩**：给某类流量配了全局代理时，**先梳理谁在用这个全局客户端**（`grep GetHTTPClient`）。翻墙代理只给"必须翻墙的流量"（TG）用，国内站点一律直连。

### 🔴 坑 16（07-28 新增）：YAML 列表项里写 `#` 不是注释；改配置后先小步验证
- **现象**：mihomo 改完配置 restart 直接崩溃循环，journalctl 报 `rules[2] [# GEOIP,lan,DIRECT,no-resolve] error: unsupported rule type # GEOIP`。
- **真相**：`rules:` 列表里有一行 `- # GEOIP,lan,DIRECT,no-resolve`——**在 YAML 列表项中间写 `#` 不构成注释**（尤其是 `- "# ..."` 带引号的形式），整行被当成规则文本。手动改配置时弄坏的；运行中的进程不重读配置，重启才暴露。
- **已修**：按内容定位行号（`grep -n "GEOIP,lan" config.yaml`）→ 按行号删除（`sed -i 'Nd'`）→ restart。
- **别再踩**：
  - 给 YAML 加注释，只能**整行注释**（行首空白后紧跟 `#`），别在 `- ` 后面写。
  - **BRE grep 不支持 `\s`**（用 `grep -P` 或 `[[:space:]]`）；在服务器终端里写复杂 sed 捕获组容易被吞转义符（本次 `\(\s*\)` 就被吃掉导致 `invalid reference \1`）。**改配置文件优先用"按内容 grep 出行号 → 按行号 sed 删除"这种零正则技巧的方式**。
  - 改完配置先 `grep` 复查内容、再 restart、再 `ss -tlnp` 看监听，三步都过才算完。

### 🔴 坑 13（07-27 新增）：搜索结果必须每次强刷（`refresh=true`），不能去缓存
- **现象**：去掉了 `refresh=true` 后，用户报告"第一次搜出来 14 条，第二次秒出 14 条，跟 mipan.so 的 378 条对不上"。
- **真相**：后端有二级缓存，搜过的关键词结果会被缓存下来。如果不去掉缓存，**第一次搜到的"坏数据"会被缓存，后续命中缓存再也看不到修复后的数据**。
- **已修**：`pansou-web/src/composables/useSearch.js` 的 `run()` 函数里 `searchParams = { kw, refresh: true, ...options }` —— **强制每次都刷新**。
- **别再踩**：
  - 看到"结果少"先怀疑缓存，确认 `refresh=true` 没被去掉。
  - 改了插件/数据源后，要清缓存卷（坑 4）才能验证修复生效。
  - 链接检测**可以**缓存（24h/6h），不能混淆"搜索结果缓存"和"链接检测缓存"两个概念。

---

## 四、标准部署流程

### 整站部署（用 pansou-web 目录）
```bash
cd ~/pansou_project
git pull origin main            # 拉最新代码

cd pansou-web
docker compose up -d --build    # --build 强制用新代码重建镜像

docker logs -f pansou           # 后端日志
docker logs -f pansou-web       # 前端日志
```

### 只清缓存（改了数据源逻辑后必须做）
```bash
cd ~/pansou_project/pansou-web
docker compose down
docker volume rm pansou-web_pansou-cache   # 先用 docker volume ls | grep cache 确认实际名
docker compose up -d
```

### 验证
```bash
curl 'http://localhost:8888/api/search?kw=庆余年' | head -c 300   # 应返回几百条
curl 'http://localhost:8888/api/health'                            # 看插件是否注册
```

---

## 五、新部署流程（07-27 包含代理）

### 完整首次部署（mihomo + pansou）
```bash
# 1. 安装 mihomo
mkdir -p /root/mihomo-config
cd /root
wget https://github.com/MetaCubeX/mihomo/releases/download/v1.18.1/mihomo-linux-amd64.gz
gunzip mihomo-linux-amd64.gz
chmod +x mihomo

# 手动下载配置（带UA伪装）
curl -L -A "ClashforWindows/0.20.39" "<订阅URL>" -o /root/mihomo-config/config.yaml

# 下载 Country.mmdb（必须命名为 Country.mmdb，C大写，无后缀）
wget -O /root/mihomo-config/Country.mmdb "https://cdn.jsdelivr.net/gh/MetaCubeX/meta-rules-dat@release/country-lite.mmdb"

# 2. 创建 systemd 服务
cat > /etc/systemd/system/mihomo.service << 'EOF'
[Unit]
Description=Mihomo (Clash Meta)
After=network.target

[Service]
Type=simple
ExecStart=/root/mihomo -d /root/mihomo-config
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
EOF
systemctl daemon-reload
systemctl enable mihomo
systemctl start mihomo

# 3. 在配置里加 external-controller（必须！）
sed -i '/^mode:/a external-controller: 127.0.0.1:9090\nsecret: ""' /root/mihomo-config/config.yaml
systemctl restart mihomo

# 4. 测试代理
curl -x socks5://127.0.0.1:7890 -s --connect-timeout 10 "https://t.me/s/tgsearchers7" | head -c 200

# 5. 部署 pansou
cd /root/pansou_project
git pull origin main
cd pansou-web
docker compose down
docker compose up -d --build

# 6. 验证 TG 搜索
curl -s "http://localhost:8888/api/search?kw=test&src=tg" | head -c 300
```

### mihomo 日常维护
```bash
# 查看状态
systemctl status mihomo

# 手动更新订阅（带UA）
curl -L -A "ClashforWindows/0.20.39" "https://47.112.97.173:5000/api/v1/client/subscribe?token=xxx" -o /root/mihomo-config/config.yaml
systemctl restart mihomo

# 测速
curl -s "http://127.0.0.1:9090/proxies/节点选择" | python3 -m json.tool

# 当前选了哪个节点
curl -s "http://127.0.0.1:9090/proxies" | python3 -c "
import json, sys
data = json.load(sys.stdin)
for name, info in data['proxies'].items():
    t = info.get('type')
    if t in ['Selector', 'URLTest', 'Fallback']:
        print(f'{name} [{t}] → 当前: {info.get(\"now\", \"无\")}')
"
```

---

## 六、给接手者的建议

1. **改代码 → `up -d --build`；只清缓存/改环境变量 → `down` + `up -d`**。别混。
2. 动 `plugin/` 下文件后，先本地确认反引号是 ASCII，再 `grep` 全仓确认没有隐藏的消费方。
3. mipan 接口是 GET，别手贱改 POST。
4. 看到"结果少"先怀疑缓存：`refresh=true` 一试便知（坑 13），确认 `refresh=true` 没被去掉。
5. 提交推送两个远程：`git push all main`（已配置 all = gitee + github；GitHub 需代理）。
6. mihomo 订阅下载**必须带 UA 伪装**（坑 8），否则拿到 vmess 列表不是 yaml。
7. mihomo 不要用 `mihomo -t` 验证配置（坑 10），直接 restart 看 status。
8. 容器访问宿主机代理必须用 `host.docker.internal:7890`（坑 11），不能用 `127.0.0.1`。
9. 看到"暂不可知"是正常的（坑 12），后端检测能力的限制。
10. **搜索结果强刷 `refresh=true`，链接检测才缓存**（坑 13）—— 两个缓存语义不同，别混淆。

---

## 七、关键配置与地址速查

### 7.1 核心服务
| 项 | 值 |
|---|---|
| 后端端口 | 8888 |
| 前端端口 | 80（nginx 反代 /api → 8888） |
| mihomo 代理端口 | 7890（SOCKS5+HTTP） |
| mihomo API 端口 | 9090（仅 127.0.0.1 监听） |
| 服务器 IP | 47.108.65.55 |
| 域名 | priclil.dpdns.org（免费域名） |

### 7.2 数据源
| 数据源 | API 参数 | 是否需要代理 |
|---|---|---|
| mipan 聚合 | `src=plugin&plugins=mipan` | ❌ |
| TG 频道（118个） | `src=tg` | ✅ 走 mihomo |
| 60 个插件 | `src=plugin&plugins=labi,zhizhen,...` | 部分需要 |

完整插件列表见 [pansou/Dockerfile](file:///d:/Desktop/pansou_project/pansou/Dockerfile) 第 68 行。

### 7.3 远程仓库
| 平台 | 仓库 |
|---|---|
| Gitee | `song-juntang/Resource_research` |
| GitHub | `sjt678/resourceResearch_pansou` |

### 7.4 当前 HEAD
`443e864`（2026-07-28）

### 7.5 关键文件
- 代理配置：`/root/mihomo-config/config.yaml`
- mmdb 文件：`/root/mihomo-config/Country.mmdb`
- mihomo 服务：`/etc/systemd/system/mihomo.service`
- 后端 compose：[pansou-web/docker-compose.yml](file:///d:/Desktop/pansou_project/pansou-web/docker-compose.yml)
- 前端搜索逻辑：[pansou-web/src/composables/useSearch.js](file:///d:/Desktop/pansou_project/pansou-web/src/composables/useSearch.js)
- 前端 UI：[pansou-web/src/views/Home.vue](file:///d:/Desktop/pansou_project/pansou-web/src/views/Home.vue)
- 完整代理指南：[Linux安装Mihomo配置代理踩坑指南.md](file:///d:/Desktop/pansou_project/Linux安装Mihomo配置代理踩坑指南.md)

---

## 八、TG 频道搜索问题诊断清单（待用户执行）

如果 TG 频道搜索返回 0 条，按顺序执行：

```bash
# 1. 拉新代码并重启
cd /root/pansou_project/pansou-web
git pull
docker compose down
docker compose up -d --build
sleep 30

# 2. 看容器日志里的 TG 抓取记录
docker compose logs pansou 2>&1 | grep -i "tg\|telegram\|channel" | tail -30

# 3. 测试容器内能否走 mihomo 代理
docker compose exec pansou curl -x socks5://host.docker.internal:7890 -s --connect-timeout 10 "https://t.me/s/tgsearchers7" | head -c 300

# 4. 测试容器内不代理是否失败（应该超时/失败）
docker compose exec pansou curl -s --connect-timeout 5 "https://t.me/s/tgsearchers7" | head -c 100

# 5. 通过后端 API 测试
curl -s "http://localhost:8888/api/search?kw=test&src=tg" | head -c 500

# 6. 完整日志
docker compose logs pansou 2>&1 | tail -50
```

**判断标准**：
| 步骤 | 期望 | 若失败说明 |
|---|---|---|
| 2 | 有 TG 抓取日志 | 后端 TG 抓取代码没运行 |
| 3 | 返回 HTML | 容器代理配置错误（坑 11） |
| 4 | 连接超时 | 国内确实被墙（正常，验证环境） |
| 5 | total > 0 | TG 抓取功能正常 |

---
